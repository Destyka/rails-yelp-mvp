<div class="container m-0 bg-secondary p-3 border-radius-2 d-flex justify-content-center">
<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection:(0..5) %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
</div>
